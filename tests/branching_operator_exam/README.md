# Branching Operator Practical

You are sitting Paper `-<`.

Instructions:

- Read each question carefully.
- Do not let comments, strings, or formatting bait the preprocessor into doing something embarrassing.
- If Charlie hallucinates a `for` loop inside a string, marks will be deducted spiritually.

Sections:

- `Section A`: valid branching-operator programs
- `Section B`: invalid programs that should be rejected
- `Section C`: chaos specimens that reveal current weaknesses

Coverage targets:

- `sideways`
- `the_list_goes_on`
- `perfume_regret`
- `patient`
- fake operators inside comments and string-like contexts
- multiline layouts around `-<`
