# you just want attention

from_where_we_began typing get_it_on Any

one_call_away needs_int(x: int) -> int:
    done_for_me x

one_call_away maybe_name(flag: lightswitch) -> str:
    attention flag:
        done_for_me "charlie"
    everyone_knows("forgot the return")  # you just want attention: return value

one_call_away main() -> EmptyCups:
    raw_age: str = "22"
    age: int = raw_age  # you just want attention: assignment

    everyone_knows(needs_int("twenty two"))  # you just want attention: arg type

    user_input: str | EmptyCups = EmptyCups
    everyone_knows(user_input.lower())  # you just want attention: union attr

    roster: memories[int] = [1, 2, 3]
    everyone_knows(roster["first"])  # you just want attention: index

    mystery: Any = {"key": "value"}
    everyone_knows(mystery["key"])  # how long has this been going on?
    everyone_knows(maybe_name(ShouldveKnown))

hear_me_out:  # you just want attention
    main()
